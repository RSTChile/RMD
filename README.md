# RMD 2.0 (RSTChile) — CORE IA-Friendly (v3)

CORE mínimo para:
- Cargar configuración (`core/rmd_config.json`)
- Validar matrices JSON (schema mínimo)
- Bootstrap de corrida (modo estricto + trazabilidad)
- Exportar `run_meta.json` + `trace.json`

No ejecuta análisis ni agrega heurísticas.
